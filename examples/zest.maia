
%skip: ' ' | '\n';

number: /int: ['0'-'9']+/ #value;

%start: number number;

x = number[0].value;

y = number[1].value;

myadd = $int a, b -> int: b == 0 ? a : myadd(a, b - 1) + 1;
%error: myadd(x, y) > 30;

// myadd = $int a -> $int -> int: $int b -> int: a + b;
// %error: myadd(x)(y) > 30;


