
struct type;
struct list_value;

struct value* sum_form_run_on_ints(
	struct type* type,
	struct list_value* list);
	
struct value* sum_form_run_on_floats(
	struct type* type,
	struct list_value* list);
	
	
